import operate from './operate'

const calculate = (obj, buttonName) => {
  const isNum = (testStr) => {
    return /\d/.test(testStr)
  }

  // obj = {
  //   total: null,
  //   next: null,
  //   operation: null
  // }

  if (isNum(buttonName)) {
    if (obj.next) {
      
    }
  }
}